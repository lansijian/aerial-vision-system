# 环境配置说明 - v10.0

## 硬件要求

### GPU配置
- **显卡驱动**: nvidia-driver-535
- **CUDA版本**: 11.7+ 或 12.2
- **cuDNN版本**: 8+
- **显存要求**: 建议4GB以上（YOLO推理需要）

### 驱动与CUDA兼容性

| 驱动版本 | 支持的CUDA版本 | 推荐配置 |
|---------|---------------|---------|
| nvidia-driver-535 | CUDA 11.7 ~ 12.2 | CUDA 11.7 + cuDNN 8 |
| nvidia-driver-530 | CUDA 11.7 ~ 12.1 | CUDA 11.7 + cuDNN 8 |
| nvidia-driver-525 | CUDA 11.7 ~ 12.0 | CUDA 11.7 + cuDNN 8 |

**当前项目配置**：
- ✅ 驱动: nvidia-driver-535
- ✅ CUDA: 11.7+ 或 12.2
- ✅ cuDNN: 8+

## 软件要求

### 基础环境
- **操作系统**: Ubuntu 18.04 LTS
- **ROS版本**: Melodic
- **Gazebo版本**: 9
- **Python版本**: 3.6+

### Python依赖
```bash
# YOLOv11相关
pip install ultralytics
pip install opencv-python
pip install numpy

# ROS相关
sudo apt-get install ros-melodic-mavros ros-melodic-mavros-extras
```

## CUDA配置验证

### 1. 检查显卡驱动
```bash
nvidia-smi
```

**预期输出**：
```
Driver Version: 535.xxx.xx    CUDA Version: 12.2
```

### 2. 检查CUDA版本
```bash
nvcc --version
```

**预期输出**：
```
cuda compilation tools, release 11.7, V11.7.xxx
```

### 3. 检查PyTorch CUDA
```python
import torch
print(f"CUDA可用: {torch.cuda.is_available()}")
print(f"CUDA版本: {torch.version.cuda}")
print(f"GPU数量: {torch.cuda.device_count()}")
if torch.cuda.is_available():
    print(f"GPU名称: {torch.cuda.get_device_name(0)}")
```

**预期输出**：
```
CUDA可用: True
CUDA版本: 11.7
GPU数量: 1
GPU名称: NVIDIA GeForce RTX 3060
```

## YOLO GPU配置

### YOLOv11自动检测
在 `scripts/yolo_v11.py` 中：
```python
import torch

class YoloDetector:
    def __init__(self):
        # 自动检测CUDA
        if torch.cuda.is_available():
            self.device = 'cuda'
            rospy.loginfo(f"✅ 使用GPU: {torch.cuda.get_device_name(0)}")
        else:
            self.device = 'cpu'
            rospy.logwarn("⚠️ CUDA不可用，使用CPU")
```

### 性能对比

| 设备 | FPS | 推理时间 | 显存占用 |
|------|-----|---------|---------|
| GPU (CUDA) | 30 | ~33ms | 2-3GB |
| CPU | 5-10 | ~100-200ms | - |

**建议**: 强烈推荐使用GPU进行YOLO检测，CPU性能不足以支持实时检测。

## 常见问题

### Q1: CUDA版本不匹配
**问题**: `RuntimeError: CUDA error: no kernel image is available for execution on the device`

**解决**:
```bash
# 重新安装与CUDA版本匹配的PyTorch
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu117
```

### Q2: cuDNN版本不匹配
**问题**: `Could not load dynamic library 'libcudnn.so.8'`

**解决**:
```bash
# 安装cuDNN
sudo apt-get install libcudnn8 libcudnn8-dev
```

### Q3: GPU未被识别
**问题**: `torch.cuda.is_available()` 返回 `False`

**解决**:
```bash
# 1. 检查驱动
nvidia-smi

# 2. 检查CUDA路径
echo $CUDA_HOME
echo $LD_LIBRARY_PATH

# 3. 设置环境变量
export CUDA_HOME=/usr/local/cuda
export LD_LIBRARY_PATH=$CUDA_HOME/lib64:$LD_LIBRARY_PATH
export PATH=$CUDA_HOME/bin:$PATH
```

## Conda环境配置（推荐）

### 创建YOLOv11环境
```bash
# 创建环境
conda create -n yolov11 python=3.8

# 激活环境
conda activate yolov11

# 安装CUDA版本的PyTorch
conda install pytorch torchvision torchaudio pytorch-cuda=11.7 -c pytorch -c nvidia

# 安装其他依赖
pip install ultralytics opencv-python
```

### launch文件中配置
在 `multi_drone_system.launch` 中已配置：
```xml
<env name="PATH" value="/home/cty/miniconda3/envs/yolov11/bin:$(env PATH)" />
<env name="PYTHONPATH" value="/home/cty/miniconda3/envs/yolov11/lib/python3.8/site-packages:$(env PYTHONPATH)" />
<env name="CONDA_DEFAULT_ENV" value="yolov11" />
```

## 性能优化建议

### 1. GPU内存优化
```python
# 在yolo_detector.py中
import torch

# 清理GPU缓存
torch.cuda.empty_cache()

# 设置内存分配策略
torch.cuda.set_per_process_memory_fraction(0.8)  # 使用80%显存
```

### 2. 批处理优化
```python
# 如果处理多个图像，使用批处理
batch_size = 2  # 根据显存调整
```

### 3. 模型优化
```python
# 使用半精度推理（FP16）
model.half()  # 减少显存占用，提升速度
```

## 环境变量配置

在 `~/.bashrc` 中添加：
```bash
# CUDA配置
export CUDA_HOME=/usr/local/cuda-11.7
export LD_LIBRARY_PATH=$CUDA_HOME/lib64:$LD_LIBRARY_PATH
export PATH=$CUDA_HOME/bin:$PATH

# cuDNN配置
export CUDNN_PATH=/usr/lib/x86_64-linux-gnu
export LD_LIBRARY_PATH=$CUDNN_PATH:$LD_LIBRARY_PATH
```

然后执行：
```bash
source ~/.bashrc
```

---

**版本**: v10.0  
**日期**: 2025-10-13  
**状态**: ✅ nvidia-driver-535 + CUDA 11.7/12.2

