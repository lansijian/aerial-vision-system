# 重构准备清单

**日期**: 2025-10-12  
**目的**: 重新构建追踪框架

---

## 📊 当前文件结构

```
catkin_ws/src/yolov11_ros/
├── scripts/
│   ├── human_tracker.py (v9.2, 812行) ← ❌ 不工作，需要重构
│   ├── waypoint_flight.py (原版, 739行) ← ✅ 工作正常
│   ├── robocup_score_cal.py (v9.2, 364行) ← ✅ 可用
│   ├── yolo_v11.py (原版) ← ✅ 工作正常
│   ├── gimbal_control.py (原版) ← ✅ 工作正常
│   ├── multi_drone_coordinator.py (原版) ← ✅ 工作正常
│   ├── human_tracker_v8.4_backup.py ← 备份
│   ├── human_tracker_before_fix.py ← 备份
│   └── 单无人机存档/
│       └── human_tracker copy.py ← ✅ 单机版，工作的
├── launch/
│   └── multi_drone_flight.launch (v9.2, 228行) ← 简化版
└── 参考代码/
    ├── XTDrone/control/yolo_human_tracking.py ← ✅ 官方，84行，工作的
    └── scripts/plan3_optimized.py ← plan3框架，1049行

保留的文档:
├── README.md ← 项目说明
├── CHANGELOG.md ← 版本历史
├── AI_MODIFICATIONS_SUMMARY.md ← AI修改摘要
├── 追踪问题完整分析与重构指南.md ← ⭐ 重构指南
├── 当前状态总结.md ← ⭐ 当前状态
└── 📋最终文件清单.txt ← 部署清单
```

---

## 🎯 问题核心

### ❌ 当前问题
**检测到人后，无人机不追踪**

### ✅ 已确认工作的功能
- 无人机起飞
- 航点飞行  
- YOLO检测
- 记分系统

### ❌ 不工作的原因（多选）
- [ ] 接口不匹配（tracking_status和cmd_vel话题）
- [ ] waypoint_flight判断逻辑bug
- [ ] human_tracker发布不及时
- [ ] 代码过于复杂，有未知bug

---

## 🔧 重构方案推荐

### 🏆 方案A: XTDrone官方架构（最推荐）

**参考**: `XTDrone/control/yolo_human_tracking.py`

**实现步骤**:
1. 创建`human_tracker_simple.py`（基于yolo_human_tracking.py）
2. 添加多机支持（drone_id参数）
3. 修改YOLO话题订阅
4. 添加ActorInfo发布
5. 外部手动启动communication或集成到launch

**代码量**: <200行

**优点**:
- ✅ 最简单
- ✅ 已验证工作
- ✅ 易于调试

**缺点**:
- ⚠️ 需要communication节点

**预计时间**: 2-3小时

**成功率**: 95%

---

### 🥈 方案B: 调试当前v9.2

**实现步骤**:
1. 在waypoint_flight中添加详细日志
2. 验证话题连接（rostopic info）
3. 监控话题数据（rostopic echo）
4. 根据日志定位具体问题
5. 逐步修复

**代码量**: 当前812行

**优点**:
- ✅ plan3算法优秀
- ✅ 已经做了很多工作

**缺点**:
- ⚠️ 代码复杂
- ⚠️ 问题定位困难
- ⚠️ 可能有多处bug

**预计时间**: 4-6小时（不确定）

**成功率**: 70%

---

### 🥉 方案C: 混合方案

**实现步骤**:
1. 保留plan3的track_target()算法
2. 使用XTDrone的简单主循环
3. 删除waypoint_flight依赖
4. 直接发布到MAVROS或communication

**代码量**: 200-300行

**优点**:
- ✅ 保留plan3优秀算法
- ✅ 简化发布逻辑

**缺点**:
- ⚠️ 仍需整合

**预计时间**: 3-4小时

**成功率**: 85%

---

## 📋 重构检查清单

### 重构前必须明确

- [ ] 是否使用waypoint_flight中间层？
- [ ] 是否使用communication节点？
- [ ] 追踪算法选择（plan3 vs XTDrone）？
- [ ] 是否需要多机协调？
- [ ] 是否需要目标锁定机制？

### 重构中必须验证

- [ ] 话题连接关系（rostopic info）
- [ ] 话题数据流（rostopic echo）
- [ ] 节点日志输出
- [ ] 每个环节独立测试

### 重构后必须测试

- [ ] 无人机能起飞
- [ ] 能航点飞行
- [ ] 检测到人
- [ ] 暂停航点
- [ ] 开始追踪
- [ ] 框在中心
- [ ] 坐标发布
- [ ] 记分系统计时

---

## 🔍 关键文件对比

### 工作的单机版（84行）

```python
# yolo_human_tracking.py核心
rospy.Subscriber("/yolov11/bounding_boxes", BoundingBoxes, darknet_callback)
cmd_vel_pub = rospy.Publisher('/xtdrone/.../cmd_vel_flu', Twist, queue_size=1)

# 主循环
while not rospy.is_shutdown():
    cmd_vel_pub.publish(twist)  # 持续发布
    # 检测丢失处理
    if find_cnt == find_cnt_last:
        if timeout > 2.0:
            twist = zero  # 停止
```

**关键**: 主循环持续发布，不管有无目标

---

### 不工作的v9.2（812行）

```python
# human_tracker.py v9.2
rospy.Subscriber(detection_topic, BoundingBoxes, self._detection_callback)
self.human_tracker_vel_pub = rospy.Publisher('/drone_0/human_tracker/cmd_vel', Twist)

# 主循环
while not rospy.is_shutdown():
    if self.tracking_active:
        self.track_target()  # 只在这里发布
    rate.sleep()

# track_target()
def track_target(self):
    target = self.get_best_color_target()
    if target is None:
        # 丢失处理，可能不发布
        return
    # 有目标才发布
    self.publish_cmd_vel(...)
```

**问题**: 只在有目标时发布，丢失时可能不发布

---

## 💡 核心建议

### 立即行动
1. **阅读**: `追踪问题完整分析与重构指南.md`
2. **选择**: 重构方案（推荐方案A）
3. **参考**: `XTDrone/control/yolo_human_tracking.py`
4. **验证**: 先单机测试，再多机测试

### 关键原则
- **保持简单** - 参考84行的官方代码
- **持续发布** - 主循环60Hz持续发布速度
- **渐进测试** - 每个功能独立验证
- **参考工作代码** - 不要盲目重写

---

## 📞 准备重构

**当前状态**: ✅ 已清理临时文件，已分析问题，已提供方案

**下一步**: 请告知选择哪个重构方案

**文档**: 
- 详细分析 → `追踪问题完整分析与重构指南.md`
- 当前状态 → `当前状态总结.md`
- 版本历史 → `CHANGELOG.md`

---

**东华大学 Astraeus队**  
**2025-10-12**

准备重构！🔧🚁

